import { DartConverter } from '../../converters';
import { LanguageEngineConfigurationBase } from '../../models';

export const DART_ENGINE_CONFIGURATION: LanguageEngineConfigurationBase = {
	enumConverter: DartConverter
};
